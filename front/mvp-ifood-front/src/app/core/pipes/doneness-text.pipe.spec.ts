import { DonenessTextPipe } from './doneness-text.pipe';

describe('DonenessTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DonenessTextPipe();
    expect(pipe).toBeTruthy();
  });
});
